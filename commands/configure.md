---
description: Configure Wire memory settings (transcript capture, etc.)
allowed-tools: [Bash]
---

# Configure Wire Memory

Run the configure script to update Wire memory settings.

```bash
node ${CLAUDE_PLUGIN_ROOT}/scripts/configure.mjs
```

After configuration, changes take effect immediately (no restart needed).
