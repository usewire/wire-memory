---
name: wire-status
description: Show Wire memory connection status and container info
---

# Wire Memory Status

Run `scripts/status.mjs` from the wire-memory plugin directory.

If not connected, suggest running /wire-connect.
